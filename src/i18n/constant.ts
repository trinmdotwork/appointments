import { InterpolationOptions } from 'i18next';

const DEFAULT_LANGUAGE = 'vi';
const SUPPORTED_LANGUAGES: Array<string> = ['en', 'vi'];
const INTERPOLATION: InterpolationOptions = { escapeValue: false };

export { DEFAULT_LANGUAGE, SUPPORTED_LANGUAGES, INTERPOLATION };
